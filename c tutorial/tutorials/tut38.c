// TYPEDEF IN C

#include <stdio.h>
int main()
{
    // syntax
    // typedef<previous_name><alias_name>;

    typedef long l;
    l a;

    // after using typedef and changing the name the original name can also be used
    return 0;
}
